export interface ShoppingSuggestion {
  name: string;
  image_url: string;
  product_page_url: string;
}

export interface IdentifiedItem {
  itemName: string;
  category: string;
  description: string;
}
